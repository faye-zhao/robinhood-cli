A Robinhood CLI that integrate Robinhood API in https://www.npmjs.com/package/robinhood
### Usage
node mfa.js             // Get mfa code. get token. add to credentials.js
node orders.js          // Get orders
//node instruments.js   // Get instrument url of a symbol
node quote.js       symbol=[xxx]
node buy.js         symbol=[xxx] price=xxx quantity=xxx
node sell.js        symbol=[xxx] price=xxx quantity=xxx
node buy.js         symbol=[xxx]  quantity=[xxx]  // price is optional
node sell.js        symbol=[xxx]  quantity=[xxx] // price is optional
node sellAll.js     symbol=[xxx]
node positions.js       // Get positions

